# Merge these entries into the project's existing pyproject.toml.
[project.optional-dependencies]
dev = [
    "redis>=5.0",
    "qasync>=0.27",
    "PyQt5>=5.15",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
